Drop your saved `Orange_Logo_transparent.png` in this folder to have it
appear automatically in the PDF report header. If it's not here, the
report still works fine — it just prints "Orange Lab" as plain text instead.
